version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Site estático (HTML/CSS/JS puro) — nenhum build necessário."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
